import { IMAGES } from 'src/assets/images';
import { ModelIcons } from 'src/common/constants';

export const IconMap: Record<ModelIcons, string> = {
  gemini: IMAGES.gemini,
  gpt: IMAGES.chatGpt,
  anthropic: IMAGES.anthropic,
  deepseek: IMAGES.deepSeek,
};
